# Java / Spring Boot + Vue → NestJS + Go 快速迁移指南

> 目标：已有 **Java / Spring Boot + Vue** 基础，在短时间内迁移到 **NestJS + Go** 技术栈。  
> 核心思路不是“重新学一遍后端”，而是：
>
> **复用已有的后端工程思维，只补语言差异、运行时差异和框架 API。**

---

# 1. 先明确我的现有技术基础

目前我的主要技术栈是：

```text
前端：
Vue

后端：
Java
Spring Boot
Spring MVC
Spring Security
MyBatis / MyBatis-Plus
MySQL
Redis
RocketMQ
XXL-Job
```

而目标岗位主要使用：

```text
NestJS
Go
```

所以真正需要迁移的是后端部分。

前端并不是从 React 转 Vue 或 Vue 转 React，而是我本身已经有 Vue 使用经验。

后端迁移可以拆成两条路线：

```text
Java / Spring Boot
        │
        ├────────────→ TypeScript → Node.js → NestJS
        │
        └────────────→ Go → HTTP → DB → Redis → 并发
```

其中：

- **NestJS 属于“框架迁移”**
- **Go 属于“语言 + 运行模型迁移”**

因此 NestJS 的迁移速度会明显快于 Go。

---

# 2. NestJS 到底是什么？

## 2.1 一句话理解 NestJS

NestJS 是一个运行在 **Node.js** 上的后端框架，主要使用：

```text
TypeScript
```

开发。

可以把它粗略理解为：

> **Node.js / TypeScript 生态里的 Spring Boot。**

它并不是 JavaScript 的某种新语言，而是一个后端 Web 框架。

整个关系是：

```text
TypeScript
    ↓
JavaScript
    ↓
Node.js
    ↓
NestJS
```

对应 Java：

```text
Java
    ↓
JVM
    ↓
Spring Boot
```

所以学习 NestJS 之前，需要先理解三件东西：

```text
TypeScript
Node.js
NestJS
```

---

# 3. TypeScript 是什么？

TypeScript 可以简单理解为：

> JavaScript + 类型系统。

例如 Java：

```java
public User getUser(Long id) {
    return userService.getById(id);
}
```

TypeScript：

```typescript
getUser(id: number): User {
  return this.userService.getById(id);
}
```

如果已经习惯 Java，TypeScript 的类型反而比较容易接受。

例如：

```typescript
interface User {
  id: number;
  name: string;
  age: number;
}
```

和 Java：

```java
public class User {

    private Long id;

    private String name;

    private Integer age;
}
```

思想非常接近。

---

# 4. Java 开发者学习 TypeScript 重点看什么？

不需要从 JavaScript 基础教程第一页开始看。

重点掌握：

```text
let / const
对象
数组
函数
class
interface
泛型
Promise
async / await
模块 import / export
装饰器 Decorator
可选参数
联合类型
类型推断
```

例如 Java 泛型：

```java
List<User> users;
```

TypeScript：

```typescript
const users: User[] = [];
```

或者：

```typescript
Array<User>
```

---

# 5. Node.js 又是什么？

这是 Java 开发者迁移 NestJS 时非常重要的一部分。

Node.js 本质上是：

> JavaScript 的服务端运行时。

Java 程序运行在：

```text
JVM
```

NestJS 程序运行在：

```text
Node.js
```

最大的认知变化之一就是：

## Java Web 常见模型

```text
请求
 ↓
Tomcat线程池
 ↓
线程处理请求
 ↓
Controller
 ↓
Service
 ↓
数据库
```

而 Node.js 更强调：

```text
请求
 ↓
Event Loop
 ↓
异步 I/O
 ↓
Promise
 ↓
回调完成
```

因此进入 NestJS 之前，我会重点补：

```text
Event Loop
Promise
async / await
异步 I/O
npm
package.json
Node.js 模块系统
```

---

# 6. Java 的 Maven 对应 Node.js 什么？

Java：

```text
pom.xml
Maven
```

Node.js：

```text
package.json
npm / pnpm / yarn
```

例如 Spring Boot 添加依赖：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>
```

NestJS：

```bash
npm install @nestjs/cache-manager
```

项目依赖最终记录在：

```json
{
  "dependencies": {
    "@nestjs/common": "...",
    "@nestjs/core": "..."
  }
}
```

所以可以建立一个简单对应：

| Java | Node.js |
|---|---|
| Maven | npm / pnpm |
| pom.xml | package.json |
| mvn install | npm install |
| Maven Central | npm Registry |
| JVM | Node.js |
| jar | Node.js 应用 |

---

# 7. NestJS 为什么适合 Spring 开发者迁移？

NestJS 最吸引 Java / Spring 开发者的一点，就是它的工程结构和 Spring 非常接近。

例如一个 NestJS 项目：

```text
src
├── app.module.ts
├── user
│   ├── user.controller.ts
│   ├── user.service.ts
│   ├── user.module.ts
│   ├── dto
│   │   └── create-user.dto.ts
│   └── entity
│       └── user.entity.ts
```

Spring Boot：

```text
src
├── controller
│   └── UserController.java
├── service
│   └── UserService.java
├── entity
│   └── User.java
├── dto
│   └── CreateUserDTO.java
└── config
```

可以看到整体思路并没有发生根本变化。

---

# 8. Spring Boot → NestJS 核心概念迁移

## Controller

Spring：

```java
@RestController
@RequestMapping("/users")
public class UserController {

    @GetMapping("/{id}")
    public User getUser(@PathVariable Long id) {
        return userService.getUser(id);
    }
}
```

NestJS：

```typescript
@Controller('users')
export class UserController {

  constructor(
    private readonly userService: UserService,
  ) {}

  @Get(':id')
  getUser(@Param('id') id: string) {
    return this.userService.getUser(id);
  }
}
```

基本是镜像关系。

---

# 9. Spring Service → NestJS Provider

Spring：

```java
@Service
public class UserService {
}
```

NestJS：

```typescript
@Injectable()
export class UserService {
}
```

NestJS 中通常称这些对象为：

```text
Provider
```

包括：

```text
Service
Repository
Factory
Helper
```

都可以作为 Provider 注册到 NestJS 的依赖注入容器。

---

# 10. Spring DI → NestJS DI

Spring：

```java
@Service
public class OrderService {

    private final UserService userService;

    public OrderService(UserService userService) {
        this.userService = userService;
    }
}
```

NestJS：

```typescript
@Injectable()
export class OrderService {

  constructor(
    private readonly userService: UserService,
  ) {}

}
```

核心思想完全一致：

```text
对象不要自己 new
        ↓
交给 IoC Container 创建
        ↓
通过构造器注入依赖
```

所以我已有的 Spring IoC / DI 思维可以直接迁移。

---

# 11. NestJS Module 是什么？

这是 NestJS 一个非常重要的概念。

例如：

```typescript
@Module({
  controllers: [UserController],
  providers: [UserService],
  exports: [UserService],
})
export class UserModule {}
```

它负责描述：

```text
这个模块有哪些 Controller
这个模块有哪些 Provider
这个模块暴露哪些 Provider
这个模块依赖哪些其他 Module
```

例如：

```text
UserModule
OrderModule
AuthModule
RedisModule
PaymentModule
```

最终：

```typescript
@Module({
  imports: [
    UserModule,
    OrderModule,
    AuthModule,
  ],
})
export class AppModule {}
```

可以把 NestJS Module 理解成：

> **显式声明某一业务模块的依赖关系和 Bean 边界。**

Spring Boot 中虽然没有完全一模一样的业务 Module 概念，但：

```text
@Configuration
@ComponentScan
Bean 注册
包结构
ApplicationContext
```

承担了一部分相似职责。

---

# 12. Spring 注解 → NestJS 装饰器

Spring 大量使用：

```java
@RestController
@Service
@Autowired
@GetMapping
@PostMapping
@RequestBody
@Valid
```

NestJS 大量使用：

```typescript
@Controller()
@Injectable()
@Get()
@Post()
@Body()
@UseGuards()
```

这些：

```typescript
@Controller()
@Injectable()
@Get()
```

在 TypeScript 中叫：

```text
Decorator
装饰器
```

所以对于 Spring 开发者来说并不陌生。

可以理解成：

```text
Java Annotation
       ↓
TypeScript Decorator
```

---

# 13. Spring MVC 和 NestJS 请求处理链路

Spring MVC：

```text
HTTP Request
      ↓
Filter
      ↓
Interceptor
      ↓
Controller
      ↓
Service
      ↓
DAO
      ↓
Database
      ↓
Controller
      ↓
Response
```

NestJS：

```text
HTTP Request
      ↓
Middleware
      ↓
Guard
      ↓
Interceptor
      ↓
Pipe
      ↓
Controller
      ↓
Provider
      ↓
Database
      ↓
Interceptor
      ↓
Response
```

这是理解 NestJS 非常关键的一张图。

---

# 14. NestJS Guard 是什么？

Guard 主要用于：

```text
认证
鉴权
权限控制
```

例如：

```typescript
@UseGuards(JwtAuthGuard)
@Get('profile')
getProfile() {
}
```

这和 Spring Security 中：

```text
FilterChain
Authentication
Authorization
```

解决的是类似的问题。

可以建立：

```text
Spring Security
        ↓
NestJS Guard + Passport + JWT
```

这样的迁移关系。

---

# 15. NestJS Pipe 是什么？

Pipe 主要负责：

```text
参数校验
参数转换
```

Spring：

```java
@PostMapping
public void create(
    @Valid @RequestBody CreateUserDTO dto
) {

}
```

NestJS：

```typescript
@Post()
create(
  @Body() dto: CreateUserDto,
) {

}
```

DTO：

```typescript
export class CreateUserDto {

  @IsString()
  username: string;

  @IsInt()
  age: number;
}
```

配合：

```typescript
ValidationPipe
```

就可以实现类似：

```text
Spring Bean Validation
Hibernate Validator
```

的效果。

---

# 16. NestJS Exception Filter

Spring 通常：

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(Exception.class)
    public Result<?> handle(Exception e) {
        ...
    }
}
```

NestJS：

```typescript
@Catch()
export class GlobalExceptionFilter
  implements ExceptionFilter {

  catch(exception: unknown, host: ArgumentsHost) {

  }
}
```

对应：

```text
@RestControllerAdvice
@ExceptionHandler
        ↓
Exception Filter
```

所以异常处理模式基本可以直接迁移。

---

# 17. NestJS Interceptor

Spring：

```java
HandlerInterceptor
```

或者：

```text
AOP
```

中经常实现：

```text
日志
耗时统计
统一响应
权限
埋点
```

NestJS：

```typescript
@Injectable()
export class LoggingInterceptor
  implements NestInterceptor {

  intercept(
    context: ExecutionContext,
    next: CallHandler,
  ) {

    return next.handle();
  }
}
```

常见用途同样是：

```text
日志
请求耗时
响应封装
缓存
异常转换
```

---

# 18. Spring Boot 和 NestJS 对照表

| Spring Boot | NestJS |
|---|---|
| `@RestController` | `@Controller()` |
| `@Service` | `@Injectable()` |
| IoC Container | DI Container |
| Bean | Provider |
| Constructor Injection | Constructor Injection |
| `@GetMapping` | `@Get()` |
| `@PostMapping` | `@Post()` |
| `@RequestBody` | `@Body()` |
| `@PathVariable` | `@Param()` |
| `@RequestParam` | `@Query()` |
| Bean Validation | ValidationPipe |
| DTO | DTO |
| Filter | Middleware |
| Spring Security | Guard / Passport |
| HandlerInterceptor | Interceptor |
| `@ControllerAdvice` | Exception Filter |
| AOP | Interceptor / Decorator |
| `application.yml` | `.env` / ConfigModule |
| Maven | npm / pnpm |
| JPA / MyBatis | TypeORM / Prisma |
| RedisTemplate | Redis Client |
| Swagger | `@nestjs/swagger` |

所以迁移 NestJS 时，我不需要重新学习：

```text
什么叫 Controller
什么叫 Service
什么叫 DTO
什么叫依赖注入
什么叫参数校验
什么叫全局异常
什么叫权限认证
```

真正需要学习的是：

> **这些概念在 NestJS 中应该怎么写。**

---

# 19. NestJS 最需要补的其实不是 Controller

对于 Spring 开发者，Controller 和 Service 基本一天就可以理解。

真正需要重点学习的是：

```text
TypeScript
Node.js 异步模型
Promise
async / await
Event Loop
Module
Provider
Guard
Pipe
Interceptor
Exception Filter
npm / pnpm
```

尤其是：

```text
Node.js Event Loop
```

这是 Java 和 Node.js 在运行模型上最大的差异之一。

---

# 20. NestJS 第一阶段目标

我不会一开始追求：

```text
掌握 NestJS 所有 API
掌握整个 Node.js 生态
```

第一阶段只要求：

> 能独立开发一个正常的 CRUD 后端服务。

包括：

```text
Controller
Service
DTO
数据库
Redis
JWT
参数校验
全局异常
Swagger
```

---

# 21. NestJS 实战项目规划

我会直接做一个小型项目，例如：

```text
nest-user-system
```

功能：

```text
用户注册
登录
JWT 鉴权
用户查询
用户修改
分页查询
Redis 缓存
参数校验
全局异常处理
Swagger
```

项目结构：

```text
src
├── auth
│   ├── auth.controller.ts
│   ├── auth.service.ts
│   ├── auth.module.ts
│   └── jwt.guard.ts
│
├── user
│   ├── user.controller.ts
│   ├── user.service.ts
│   ├── user.module.ts
│   └── dto
│
├── common
│   ├── filters
│   ├── guards
│   ├── interceptors
│   └── decorators
│
└── app.module.ts
```

通过这个项目基本可以覆盖岗位日常开发最常见的 NestJS 能力。

---

# 22. Go 和 NestJS 不一样

NestJS 对我来说主要是：

```text
Spring Boot
     ↓
NestJS
```

属于框架迁移。

Go 则需要重新建立：

```text
语言基础
运行时模型
并发模型
工程习惯
```

所以我不会直接说：

> 两周掌握 Go。

更准确的目标应该是：

> **两周内达到可以阅读 Go 项目、理解主要调用链，并完成普通 CRUD / Redis / HTTP 类型需求。**

这对于实习岗位更加现实。

---

# 23. Java 开发者学习 Go 的优势

虽然 Go 是新的语言，但很多基础能力并不会清零。

已有经验：

```text
HTTP
REST API
Controller / Service
MySQL
Redis
MQ
事务
缓存
并发
锁
Context 类思想
异常处理
工程分层
```

这些都是后端通用能力。

真正变化的是：

```text
Java语法
    ↓
Go语法

JVM
    ↓
Go Runtime

Thread
    ↓
Goroutine

Blocking Queue / Future
    ↓
Channel

Spring Context
    ↓
显式依赖组织
```

---

# 24. Go 第一阶段学什么？

第一阶段不要一上来研究：

```text
GC源码
GMP调度源码
runtime源码
高级并发模式
```

先学习：

```text
变量
函数
struct
interface
slice
map
pointer
error
defer
package
module
```

例如 Java：

```java
public class User {

    private Long id;

    private String name;
}
```

Go：

```go
type User struct {
    ID   int64
    Name string
}
```

---

# 25. Go 最大的语言差异：错误处理

Java：

```java
try {
    userService.create(user);
} catch (Exception e) {
    ...
}
```

Go 更常见：

```go
user, err := userService.Create()

if err != nil {
    return err
}
```

也就是说 Go 大量使用：

```text
显式错误返回
```

而不是：

```text
Exception
try/catch
```

这是 Java 开发者必须快速适应的地方之一。

---

# 26. Go 的 interface

Java：

```java
public interface UserService {

    User getUser(Long id);

}
```

Go：

```go
type UserService interface {
    GetUser(id int64) (*User, error)
}
```

但是 Go 有一个很重要的区别：

> **不需要显式写 implements。**

只要方法满足接口：

```go
type UserServiceImpl struct {
}
```

实现：

```go
func (s *UserServiceImpl) GetUser(
    id int64,
) (*User, error) {

}
```

就自动满足：

```go
UserService
```

接口。

---

# 27. Go Web 从哪里开始？

建议首先理解标准库：

```text
net/http
```

知道 Go 原生 HTTP 服务是什么样的。

之后再学习项目实际使用的 Web 框架。

例如常见：

```text
Gin
Echo
Fiber
```

如果目标公司项目使用 Gin，就直接重点学习 Gin。

例如：

```go
router.GET("/users/:id", func(c *gin.Context) {

    id := c.Param("id")

})
```

整体仍然是：

```text
HTTP Request
      ↓
Router
      ↓
Handler
      ↓
Service
      ↓
Repository
      ↓
Database
```

和 Spring MVC 并没有本质区别。

---

# 28. Java Controller → Go Handler

Spring：

```java
@GetMapping("/{id}")
public User getUser(
    @PathVariable Long id
) {

    return userService.getUser(id);
}
```

Go：

```go
func GetUser(c *gin.Context) {

    id := c.Param("id")

    user, err := userService.GetUser(id)

    if err != nil {
        return
    }

    c.JSON(200, user)
}
```

变化主要是：

```text
框架 API
语言语法
错误处理
```

业务思维仍然相同。

---

# 29. Go 数据库

第一阶段我会先掌握：

```text
database/sql
```

的基础思想，再根据项目使用：

```text
GORM
sqlx
其他 ORM / 数据访问框架
```

如果使用 GORM：

```go
var user User

result := db.First(&user, id)

if result.Error != nil {
    return nil, result.Error
}
```

对于有 MyBatis / MySQL 使用经验的人来说：

```text
表设计
SQL
索引
事务
分页
慢查询
```

这些知识全部可以继续复用。

---

# 30. Go Redis

Java：

```java
redisTemplate.opsForValue()
    .set(key, value);
```

Go 本质上仍然是：

```text
Redis Command
```

例如：

```go
client.Set(
    ctx,
    key,
    value,
    time.Minute,
)
```

Redis 的：

```text
String
Hash
List
Set
ZSet
TTL
SETNX
缓存穿透
缓存击穿
分布式锁
```

并不会因为换语言而改变。

所以这部分重点不是重新学 Redis，而是：

> 学 Go Redis Client 的 API。

---

# 31. Go Context

这是 Go 后端非常重要的概念。

经常会看到：

```go
func GetUser(
    ctx context.Context,
    id int64,
) (*User, error)
```

Context 通常负责传播：

```text
请求生命周期
超时
取消信号
Trace 信息
请求范围数据
```

例如：

```go
ctx, cancel :=
    context.WithTimeout(
        context.Background(),
        2*time.Second,
    )

defer cancel()
```

对后端开发非常重要。

---

# 32. Go Goroutine

Java：

```java
executor.submit(() -> {
    sendMessage();
});
```

Go：

```go
go sendMessage()
```

这里：

```text
go
```

会启动一个：

```text
goroutine
```

可以粗略理解为：

> 一种非常轻量级的并发执行单元。

但不能简单等价成：

```text
goroutine = Java Thread
```

因为其调度方式和成本都不同。

---

# 33. Go Channel

Java 并发中经常有：

```text
BlockingQueue
Future
CompletableFuture
线程通信
```

Go 中一个核心工具是：

```text
channel
```

例如：

```go
ch := make(chan int)

go func() {
    ch <- 10
}()

value := <-ch
```

核心思想是：

```text
goroutine
    ↓
channel
    ↓
goroutine
```

通过 Channel 在不同 Goroutine 之间传递数据。

---

# 34. Go 并发第一阶段掌握范围

对于实习岗位，我第一阶段重点掌握：

```text
goroutine
channel
select
sync.WaitGroup
sync.Mutex
context.Context
```

先能看懂常见并发代码。

之后再深入：

```text
GMP
goroutine 调度
channel 底层
sync.Map
atomic
内存模型
```

---

# 35. Java → Go 思维映射

| Java | Go |
|---|---|
| class | struct |
| interface | interface |
| implements | 隐式实现 |
| Exception | error |
| try/catch | `if err != nil` |
| Thread | goroutine |
| Executor | goroutine / worker pool |
| BlockingQueue | channel |
| synchronized | Mutex |
| CompletableFuture | goroutine + channel |
| ThreadLocal | Context / 显式参数 |
| Maven | Go Modules |
| `pom.xml` | `go.mod` |
| Spring MVC | net/http / Gin 等 |
| MyBatis / JPA | database/sql / GORM / sqlx |
| RedisTemplate | Go Redis Client |

---

# 36. 两周迁移计划

## 第 1 天：TypeScript

目标：

```text
可以阅读 NestJS 代码
```

学习：

```text
基础类型
function
class
interface
generic
import / export
Decorator
Promise
async / await
```

当天写一个：

```text
User
UserService
UserController
```

的小 Demo。

---

## 第 2 天：Node.js

重点：

```text
Node.js 是什么
Event Loop
异步 I/O
Promise
async / await
npm
package.json
环境变量
```

尤其理解：

```text
为什么 Node.js 不应该在请求线程里做大量 CPU 密集计算
```

---

## 第 3 天：NestJS 基础

学习：

```text
Module
Controller
Provider
DI
DTO
```

建立：

```text
UserModule
UserController
UserService
```

---

## 第 4 天：NestJS 请求链路

学习：

```text
Middleware
Guard
Pipe
Interceptor
Exception Filter
```

重点建立 Spring 映射。

---

## 第 5 天：NestJS 数据库

完成：

```text
MySQL
用户新增
用户查询
用户修改
用户删除
分页
```

---

## 第 6 天：NestJS 鉴权

完成：

```text
登录
密码校验
JWT
Guard
用户身份解析
接口权限
```

对应已有 Spring Security 经验。

---

## 第 7 天：NestJS Redis + Swagger

完成：

```text
Redis 缓存
TTL
缓存查询
Swagger
统一响应
全局异常
参数校验
```

第一周结束时应该已经可以独立完成：

```text
NestJS CRUD 项目
```

---

# 37. 第二周 Go

## 第 8 天：Go 基础

学习：

```text
变量
函数
struct
interface
slice
map
pointer
package
error
defer
```

重点适应：

```go
if err != nil {

}
```

---

## 第 9 天：Go 工程结构

学习：

```text
go mod
package
项目目录
依赖管理
配置
日志
```

能够运行：

```bash
go run .
```

和：

```bash
go build
```

---

## 第 10 天：Go HTTP

学习：

```text
net/http
Router
Handler
JSON
参数解析
```

如果项目使用 Gin，则当天同步学习 Gin。

完成：

```text
GET /users/:id

POST /users
```

---

## 第 11 天：Go MySQL

完成：

```text
连接数据库
CRUD
事务
分页
```

已有 MySQL / MyBatis 经验可以直接复用。

---

## 第 12 天：Go Redis

完成：

```text
Redis
缓存
TTL
SETNX
```

把之前 Java 项目里写过的 Redis 场景迁移一个出来。

---

## 第 13 天：Go 并发

学习：

```text
goroutine
channel
select
WaitGroup
Mutex
Context
```

写几个简单 Demo，例如：

```text
并发调用三个接口
等待所有结果返回
设置2秒超时
```

---

## 第 14 天：完整小项目

最终做：

```text
go-user-service
```

包含：

```text
HTTP API
MySQL
Redis
JWT
统一错误处理
Context
简单 goroutine
日志
```

最后做到：

> 能从 HTTP 请求一路追踪到数据库调用，并能独立修改一个普通业务需求。

---

# 38. 两周后的能力目标

我不会把目标定义成：

```text
两周精通 NestJS
两周精通 Go
```

这个并不现实。

我的目标会定义为三个层次。

## NestJS

达到：

```text
可以独立写业务接口
```

包括：

```text
Controller
Service
Module
DTO
MySQL
Redis
JWT
Swagger
异常处理
参数校验
```

---

## Go

达到：

```text
可以阅读项目
可以跟调用链
可以完成普通需求
```

包括：

```text
HTTP
CRUD
MySQL
Redis
Context
基础 Goroutine
```

---

## 工作后继续深入

再针对项目补：

```text
Go Runtime
GMP
Channel
并发模型
性能优化

Node Event Loop
NestJS 生命周期
NestJS DI 原理
微服务
消息队列
```

---

# 39. 为什么我认为这个迁移是可行的？

因为我要迁移的并不是：

```text
从零学习后端开发
```

而是：

```text
已有后端知识
        ↓
映射到新的语言和框架
```

比如：

```text
HTTP
REST
Controller
Service
DTO
DI
Redis
MySQL
事务
缓存
MQ
权限
参数校验
异常处理
```

这些概念不会因为：

```text
Java → TypeScript → Go
```

发生本质变化。

变化的是：

```text
语言语法
框架 API
运行时模型
工程规范
```

所以我的学习方式不会是重新完整看一本：

```text
《NestJS 从入门到精通》
《Go 从入门到精通》
```

而是：

```text
已有概念
    ↓
寻找对应实现
    ↓
立即写 Demo
    ↓
放进完整项目
```

通过这种方式提高迁移速度。

---

# 40. 面试时可以怎么回答

如果面试官问：

> 你的技术栈是 Java / Spring，但我们的岗位是 NestJS + Go，你准备怎么迁移？

可以回答：

> 我目前的前端技术栈其实主要是 Vue，后端是 Java 和 Spring Boot。针对 NestJS 和 Go，我会把它们分成两个不同的迁移路径。
>
> NestJS 本质上运行在 Node.js 上，主要使用 TypeScript，所以我会先补 TypeScript、Node.js 的异步模型和包管理，再把已有的 Spring 经验迁移过去。NestJS 对 Spring 开发者其实比较友好，比如 Spring 的 Controller、Service、依赖注入、参数校验、全局异常处理，在 NestJS 里分别可以对应 Controller、Provider、DI、Pipe 和 Exception Filter，Guard、Interceptor 这些也能和 Spring Security、拦截器以及 AOP 的经验建立对应。所以这一部分我认为主要是框架 API 和 Node.js 运行模型的迁移，不是重新学习后端。
>
> 我会在第一周直接做一个 NestJS 小项目，把登录鉴权、DTO 校验、MySQL、Redis 缓存、全局异常处理和 Swagger 完整跑通，通过项目快速熟悉它的开发方式。
>
> Go 对我来说跨度更大一些，所以我不会说短时间内就能精通。我的目标是先达到能够阅读项目和完成普通业务需求的程度。会先学习 struct、interface、error、slice、map 这些语言基础，然后学习 HTTP、MySQL、Redis，再重点补 goroutine、channel、Context、Mutex 这些 Go 后端比较重要的内容。
>
> 我本身已经有 HTTP、数据库、Redis、并发、缓存、消息队列这些后端开发经验，所以我认为迁移过程中最大的成本主要是语言语法、运行时模型以及框架 API，而不是重新建立整个后端知识体系。两周我的目标不是宣称掌握两门新技术，而是做到 NestJS 可以独立写正常业务接口，Go 可以读懂项目调用链并完成基础需求，之后再结合实际项目继续深入。

---

# 41. 最重要的迁移原则

最后整个学习路线可以浓缩成：

```text
NestJS：

Spring经验
   ↓
TypeScript
   ↓
Node.js
   ↓
NestJS
   ↓
完整 CRUD 项目
```

以及：

```text
Go：

Java后端经验
   ↓
Go语法
   ↓
HTTP
   ↓
MySQL / Redis
   ↓
goroutine / channel
   ↓
Context
   ↓
完整业务项目
```

真正需要向面试官证明的并不是：

> **“我现在已经会 NestJS 和 Go。”**

而是：

> **“虽然当前主技术栈是 Java / Spring Boot，但我知道哪些能力可以复用、哪些差异必须补齐，并且有明确的两周落地路径，可以快速达到参与实际业务开发的水平。”**

这才是实习岗位最有说服力的迁移能力。