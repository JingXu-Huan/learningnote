# 11.4 预拉取镜像

```bash
sudo kubeadm config images pull \
  --config "$HOME/kubeadm-init.yaml"
```

如果成功，会下载：

* kube-apiserver
* kube-controller-manager
* kube-scheduler
* etcd
* CoreDNS
* kube-proxy
* pause

