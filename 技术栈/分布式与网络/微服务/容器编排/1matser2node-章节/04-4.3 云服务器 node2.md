# 4.3 云服务器 node2

```bash
sudo hostnamectl set-hostname node2
```

重新打开终端，确认：

```bash
hostname
```

三台机器应分别输出：

```text
k8s-master
node1
node2
```

每个 Kubernetes 节点必须有唯一的主机名、MAC 地址和 `product_uuid`。

---

# 五、安装 Tailscale 私有网络

下面的操作在三台机器上都执行。

