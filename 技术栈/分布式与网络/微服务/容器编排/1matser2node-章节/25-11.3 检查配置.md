# 11.3 检查配置

```bash
cat "$HOME/kubeadm-init.yaml"
```

特别确认：

```yaml
advertiseAddress: "100.x.x.x"
controlPlaneEndpoint: "100.x.x.x:6443"
value: "100.x.x.x"
```

不能是：

```text
127.0.0.1
192.168.x.x
云服务器公网IP
```

