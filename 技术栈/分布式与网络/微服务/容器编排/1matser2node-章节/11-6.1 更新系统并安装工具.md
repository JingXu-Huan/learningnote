# 6.1 更新系统并安装工具

```bash
sudo apt-get update

sudo apt-get install -y \
  ca-certificates \
  curl \
  gpg \
  apt-transport-https \
  conntrack \
  socat \
  ipset \
  jq \
  netcat-openbsd
```

不建议在搭建过程中执行跨 Ubuntu 大版本升级。

---

