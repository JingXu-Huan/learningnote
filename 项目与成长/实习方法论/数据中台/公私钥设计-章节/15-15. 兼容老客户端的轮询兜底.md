# 15. 兼容老客户端的轮询兜底

如果老客户端没有传 `X-Key-Id`，服务端可以短期兼容：

```text
如果请求带了 X-Key-Id：
    使用 AppId + KeyId 精准查询 publicKey

如果请求没有 X-Key-Id：
    只尝试该 AppId 下最近的 1 到 2 把启用 key
```

注意：

```text
不要无限轮询所有历史 publicKey
```

建议最多尝试：

```text
1. 当前 key
2. 上一把 key
```

这只能作为过渡方案，不应作为长期设计。

---

