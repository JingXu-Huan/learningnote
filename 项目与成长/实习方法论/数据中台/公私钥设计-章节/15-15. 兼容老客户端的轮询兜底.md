# 15. 已移除：轮询兜底方案

本文档不再保留轮询密钥设计。

统一规则如下：

```text
请求必须携带 X-App-Id 与 X-Key-Id
服务端必须使用 AppId + KeyId 精准定位 publicKey
未携带 X-Key-Id 的请求直接按参数错误处理
```

这意味着验签路径只有一条：

```text
AppId + KeyId -> PublicKey -> Verify
```

---

