# 12. Query 参数排序规则

例如原始 query 是：

```text
b=2&a=1
```

签名前必须统一排序为：

```text
a=1&b=2
```

否则客户端签名内容和服务端签名内容可能不一致。

推荐规则：

```text
1. 按参数名 key 字典序升序排序
2. 如果同一个 key 有多个 value，value 也按字典序排序
3. 参数名和值都使用统一的 URL 编码规则
4. 空 query 使用空字符串
```

---

