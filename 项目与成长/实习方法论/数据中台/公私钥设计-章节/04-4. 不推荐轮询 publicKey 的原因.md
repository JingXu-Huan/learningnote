# 4. 不推荐轮询 publicKey 的原因

| 问题 | 说明 |
|---|---|
| 性能差 | RSA 验签比 HMAC 更重，多把 key 轮询会浪费 CPU |
| 逻辑不清晰 | 服务端不知道客户端本来想用哪一把 key |
| 不利于密钥轮换 | 新旧 key 并存时不好管理 |
| 不利于审计 | 日志里不好记录具体使用了哪把 key |
| 风险扩大 | 一个 AppId 下挂太多历史 key，会扩大安全边界 |

因此，推荐在请求头中增加：

```http
X-Key-Id: key_202607
```

这样服务端可以通过：

```text
AppId + KeyId
```

精准找到对应的 publicKey，只验签一次。

---

