# 14. 密钥轮换方案

为了方便密钥更换，推荐一个 AppId 支持多把 key。

## 14.1 正常状态

```text
app_001 + key_202607 -> 当前启用 publicKey
```

客户端请求时带：

```http
X-App-Id: app_001
X-Key-Id: key_202607
```

---

## 14.2 轮换状态

新增一把 key：

```text
app_001 + key_202607 -> 旧 publicKey，暂时仍启用
app_001 + key_202608 -> 新 publicKey，启用
```

客户端逐步切换到新 key。

---

## 14.3 下线旧 key

当确认所有客户端都切换完成后：

```text
app_001 + key_202607 -> 禁用
app_001 + key_202608 -> 启用
```

禁用旧 key 后，旧私钥签出来的请求全部拒绝。

---

