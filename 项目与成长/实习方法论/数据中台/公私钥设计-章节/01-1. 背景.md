# 1. 背景

当你的系统需要作为一个独立 API 服务，供外部用户、第三方系统或企业客户调用时，需要解决几个核心问题：

1. 如何识别调用方是谁；
2. 如何确认请求确实由合法调用方发出；
3. 如何防止请求参数被篡改；
4. 如何防止抓包后重复请求，也就是重放攻击；
5. 如何支持一个 AppId 下多把公钥，方便密钥轮换。

如果采用 RSA 公私钥方案，推荐使用：

```text
AppId + KeyId + RSA 签名 + Timestamp + Nonce
```

其中：

| 字段 | 作用 |
|---|---|
| `AppId` | 标识调用方应用 |
| `KeyId` | 标识当前使用哪一把公钥 |
| `PrivateKey` | 调用方自己保存，用于签名 |
| `PublicKey` | 服务端保存，用于验签 |
| `Timestamp` | 防止旧请求长期有效 |
| `Nonce` | 防止同一个请求被重复提交 |
| `Signature` | 调用方用私钥生成的签名 |

---

