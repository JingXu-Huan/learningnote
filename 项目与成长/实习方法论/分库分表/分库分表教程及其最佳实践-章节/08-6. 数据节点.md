# 6. 数据节点

一个数据节点表示一个具体的数据源和物理表组合。

例如：

```text
ds_1.t_order_2
```

如果系统采用 2 个数据库、每个数据库 4 张订单表，那么一共有 8 个数据节点。

在 ShardingSphere 中，可以使用行表达式描述这些节点：

```yaml
actualDataNodes: ds_${0..1}.t_order_${0..3}
```

ShardingSphere 的 YAML 分片规则支持逻辑表、真实数据节点、分库策略、分表策略、绑定表、主键生成器和分片算法等配置。

---

