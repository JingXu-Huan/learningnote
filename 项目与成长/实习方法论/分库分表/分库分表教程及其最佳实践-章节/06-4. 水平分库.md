# 4. 水平分库

水平分库是将同一业务的数据拆分到多个数据库实例。

例如：

```text
ds_0
└── 部分用户的订单

ds_1
└── 另一部分用户的订单
```

按照用户编号分库：

```text
user_id % 2
```

那么：

```text
user_id = 1001 → ds_1
user_id = 1002 → ds_0
```

水平分库可以分散：

* 写入压力；
* 查询压力；
* 数据库存储；
* 数据库连接；
* CPU 和磁盘 I/O。

---

