# 10. 广播表

字典表、地区表、状态配置表等数据量较小，但可能被所有分片表关联查询。

这类表可以在每个数据库中保存一份，称为广播表。

例如：

```text
ds_0.t_region
ds_1.t_region
```

配置如下：

```yaml
- !BROADCAST
  tables:
    - t_region
```

对广播表执行查询时，可以从本地数据库读取；执行修改时，中间件需要将操作发送到所有数据源。ShardingSphere 在 YAML 规则中使用 `!BROADCAST` 声明广播表。

---

