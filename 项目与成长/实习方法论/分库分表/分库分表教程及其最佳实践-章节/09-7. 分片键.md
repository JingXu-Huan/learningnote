# 7. 分片键

分片键是决定数据存储位置的字段。

常见的分片键包括：

* `user_id`；
* `order_id`；
* `tenant_id`；
* `merchant_id`；
* `device_id`；
* `create_time`。

例如：

```text
按照 user_id 分库
按照 order_id 分表
```

在这种设计中，`user_id` 是分库键，`order_id` 是分表键。

分片键是整个分库分表设计中最重要的决策。一旦选错，后期可能出现大量全路由查询、跨库事务和数据迁移问题。

---

