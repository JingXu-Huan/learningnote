# 文件上传

## 服务器端代码

```java
@slf4j
@RestController
public class UploadController {
	@PostMapping("/upload")
	//注意此处的 file 和前端 要保持一致
public Result handleFileUpload(String name, Integer age, MultipartFile file); {
	log.info(“文件上传:{}",file);
	return Result.success();
	}
}
//存储是临时的
```

